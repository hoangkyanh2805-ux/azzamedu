# Skill index

## In-repo skills (`.ai/skills/`)

| Skill | Purpose |
|-------|---------|
| `learnhouse-production-sync` | Deploy LearnHouse to iNET VPS + sync local→prod (see `PRODUCTION-DEPLOY-RUNBOOK.md`) |

## External references

Install into `.codex/skills/`:

```bash
npx skills add coreyhaines31/marketingskills --skill lead-magnets page-cro copywriting email-sequence form-cro pricing-strategy
npx skills add addyosmani/web-quality-skills
```

See `docs/repo_skill_map.md` for full routing table.
